<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:textColorSecondary">#ffffff</item>
    </style>
    <style name="imb_style">
        <item name="android:background">@drawable/round_corners</item>
        <item name="android:padding">8dp</item>
    </style>
    <style name="Theme.Talamonti" parent="Theme.AppCompat.Light.NoActionBar.FullScreen">
        <item name="android:background">@color/transparent</item>
        <item name="android:windowBackground">@drawable/gradient</item>
        <item name="android:colorBackground">@color/transparent</item>
        <item name="android:itemBackground">@color/primary_back</item>
        <item name="android:textColor">@color/edit_stroke</item>
        <item name="android:editTextColor">@color/primary_stroke</item>
        <item name="android:textColorHint">@color/hint_stroke</item>
    </style>
</resources>